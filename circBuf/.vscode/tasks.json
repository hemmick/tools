{
  // See https://go.microsoft.com/fwlink/?LinkId=733558 
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
      {
          "label": "build",
          "type": "shell",
          "command": "g++ test.c circular_buffer.c -o ${workspaceFolderBasename}.out"
      },
      {
          "label": "run",
          "type": "shell",
          "command": "${workspaceRoot}/${workspaceFolderBasename}.out"
      }
  ]
}
